# 模板 · 排版规范：藏蓝+红 · 章节分隔（实测版 v1.0，2026-09-17）

> **来源**：2026-09-16《百万粉丝博主去上学了，她把爱好读成了专业》发布页内联样式逆向实测。
> **不是目测**：以下所有数值都是从页面 HTML 的内联 `style` 里提取的真实值。
> **配套件**：`assets/` 下 4 张分隔条（壹/贰/叁 + THE END）。

---

## 一、视觉变量总表（照抄即可）

| 项目 | 实测值 | 备注 |
|---|---|---|
| 容器 | `max-width:677px; margin:0 auto` | 微信正文列宽 |
| 背景 | `#FFFFFF` | **必须纯白**，见第三节 |
| 字体族 | `"PingFang SC NEW", system-ui, -apple-system, BlinkMacSystemFont, "Helvetica Neue", "Hiragino Sans GB", "Microsoft YaHei UI", "Microsoft YaHei", Arial, sans-serif` | 81 处，全篇统一 |
| 正文字号 | `15px` | 与红白色系主题「15px 不可改」一致 |
| 行高 | `1.75em` | 82 处 |
| 字间距 | `0.544px` | 86 处（编辑器 0.5px 的落地值） |
| 段左右边距 | `margin: 0 16px` | 61 处；文本与图片同一边距 |
| 段间距 | **空段（`<br>`）撑开 ≈26px** | 不是 margin-bottom |
| 正文色 | `#595959` | rgb(89,89,89)，36 处 |
| 层级色 | `#1F4E6E` 藏蓝 + `font-weight:bold` | rgb(31,78,110)，11 处 |
| 强调色 | `#FF4C00` 橙红 + bold | 仅用于人名/关键词首现（本篇：游天祺） |
| 结尾句 | `#595959` + bold | 不加藏蓝，与层级行区分 |

---

## 二、层级规则：藏蓝加粗 = 唯一层级标记

全篇 11 处藏蓝加粗，**字号一律仍是 15px**（没有放大）。它同时承担三种角色：

1. **署名** —— `作者 | 小铭`
2. **小标题** —— 含问句标题，如 `"翠蛇大将军"这名字怎么来的？`
3. **正文内层级行** —— `我特别想让家长看到这一段。` / `第一，…` / `第二，…` / `第三，…`

> 这是这套排版的性格：**不用字号差、不用色块、不用边框来做层级，只用一种颜色的加粗。** 所以全篇视觉非常平，层级靠位置和颜色区分。套用时不要自己加字号或加粗色块，一加就破。

---

## 三、图片规格与落位

| 图 | 尺寸 | 比例 | 落位 |
|---|---|---|---|
| 头图 | 1800×766 | 2.35:1 | 正文最上方，**在「作者｜小铭」之前**，居中、无边距 |
| 人物/配图 | 1080×639 | 约 1.69:1 | 第 3 段之后，带 16px 边距 |
| 章节分隔条 壹/贰/叁 | 2160×340（主图，**6.35:1**） | 6.35:1 | 每章开头，**替换该章第一个小标题之前**；**显示宽 62%** → 渲染 400×63 |
| THE END 条 | 2160×161（**13.42:1**） | 13.42:1 | 结尾升维句之后、「原创不易」之前；**显示宽 62%** → 渲染 400×30 |

**比例与宽度修订（2026-09-17）**：原始素材是 2160×540（4:1），在 645px 正文列宽下渲染高 **161px ≈ 6 行正文**，章头压得太重。实测发现两类图的成因不同：

- **壹/贰/叁**：内容占画布 94%，几乎没有可裁的留白——图形本身占满整幅。变扁只能「内容等比缩小 → 贴到更扁的画布上居中」，两侧会留白。现取画布高 340 → **6.35:1**。
- **END 条**：内容只占原画布 **12%**，上下 474px 全是空白，可以直接裁。现裁到 161 高 → **13.42:1**。

再叠加**显示宽度收到 62%**（全宽 → 400px），最终章头条 161px → **63px**、END 161px → **30px**。

- 改比例：`tools/make_separators.py`（`--head` / `--end-band` / `--restore`）
- 改宽度：`build.py` 顶部的 `DIVIDER_WIDTH`（100 = 与正文同宽，62 = 当前）
- 图仍居中：显示宽度收窄后 `<p>` 上的 `text-align:center` 负责居中，不要改成左对齐

**⚠️ 白底坑**：4 张分隔条都是**无透明通道的纯白底 PNG**（`hasAlpha=no`）。所以正文背景必须 `#FFFFFF`；如果换成 `#F5F5F7`（封面品牌底色），分隔条会露出一个白色方块。这一条是硬约束。

**图片写法（与正文同边距）**
```html
<p style="margin:0 16px;text-align:center;line-height:0;"><img src="..." style="max-width:100%;height:auto;display:block;margin:0 auto;"></p>
```
头图不缩进，用：
```html
<section style="text-align:center;line-height:0;"><img src="..." style="max-width:100%;height:auto;display:block;margin:0 auto;"></section>
```

---

## 四、正文片段写法

```html
<!-- 普通段落 -->
<p style="margin:0 16px;">正文……</p>
<!-- 段间距：一个空段 -->
<p style="margin:0 16px;"><strong><br></strong></p>
<!-- 层级行（小标题 / 第一二三 / 作者署名） -->
<p style="margin:0 16px;"><strong><span style="color:#1F4E6E;">藏了三年的爱好</span></strong></p>
<!-- 人名/关键词首现 -->
<span style="color:#FF4C00;font-weight:bold;">游天祺</span>
```

---

## 五、图文序列（本篇实际排布，可直接当骨架）

```
[头图 2.35:1]
作者 | 小铭                                   ← 居中
段 · 段
[人物图]
段
──── 分隔条 壹 ────
小标题 · 段
小标题 · 段 · 段 · 段
小标题 · 段 · 段 · 段 · 段
──── 分隔条 贰 ────
层级行（我特别想让家长看到这一段。）· 段
小标题 · 段 · 段 · 段 · 段
──── 分隔条 叁 ────
小标题（家长能从这件事里拿走什么）
层级行（第一/二/三）各带 1 段
结尾升维句（#595959 加粗，左对齐）
──── THE END ────
原创不易，感谢有你！                          ← 居中
一起转发出去，让更多人看到。                    ← 居中
（正文到此结束，不加签名行）
```

**居中惯例（2026-09-17 定）**：全篇正文一律左对齐，**只有三类行居中** —— ① 署名「作者 | 小铭」；② END 条之后的两句收尾语；③ 图片。居中写法就是给该 `<p>` 加 `text-align:center`，字号、颜色、字间距都不动。

**尾部不加签名行（2026-09-17 定）**：正文最后一句就是「一起转发出去，让更多人看到。」，不再附「我是小铭，专注普通家庭升学规划，用数据说话」这类签名。原文本身也没有。

**规律**：全篇 **5 个小标题，只切 3 个章**。分隔条不是"每个小标题都给"，而是**按叙事段落切**——壹=爱好与高三，贰=方法（怎么找到专业），叁=给家长的动作。分隔条过度使用会让文章看起来像目录；每 800 字左右切一次是这套的实际密度。

---

## 六、套用时不要继承的地方

1. **正文末尾不加签名行。** 「我是小铭，专注普通家庭升学规划，用数据说话」这类句子**不要加**（2026-09-17 用户定）。2026-09-16 那篇原文正文到「一起转发出去，让更多人看到。」就结束了，本身也没有签名。不要照红白色系模板的签名区约定往这套排版里搬。
2. **「一起转发出去，让更多人看到。」要删掉。** 这是转发引导 footer，正是你红白色系模板里写明的「朱雀 footer 红线」（严禁点赞/在看/转发三连）。当模板用会把这个高风险件一起带走。
3. 「原创不易，感谢有你！」属于可留可删的通用句，但它和上面那句连在一起就是成套的公众号尾巴，建议整组删。

---

## 七、成品对照

- HTML：`03-排版模板/预览-游天祺-排版还原.html`（图片已 base64 内嵌，全选复制图片不丢）
- 页面实测总高：**3927px**（900px 视窗宽度下）
- 已用无头 Chrome 截图 + PDF 分段复检：头图、人物图、三条分隔条、END 条均正常渲染

## 八、图片要能复制，必须 base64 内嵌

**公众号编辑器粘不了 `file://` 绝对路径和 `assets/xxx.png` 相对路径的图片——复制过去图片直接丢。** 只有把图片转成 base64 `data:` URI 写进 `src`，全选复制时图片才会跟着走。

写法：
```python
import base64, mimetypes
mime = mimetypes.guess_type(path)[0] or "image/png"
uri = "data:%s;base64,%s" % (mime, base64.b64encode(open(path,'rb').read()).decode())
```

代价是文件变大：4 张分隔条 → 约 180KB；再加头图和人物图 → 约 485KB。所以**源文件用相对路径保持可读，另生成一份 base64 版专门用来复制**，别混成一份。

## 九、可复用仓库

- GitHub：https://github.com/dululu/gzh-navy-red-layout （public）
- 内容：`index.html`（自包含可复制）/ `index.relative.html`（可读）/ `layout-spec.md` / `build.py` / `assets/`
- **改结构要改 `build.py` 再重新生成，不要直接改 HTML**
- 含第三方照片的还原页（游天祺那版）只留本地，不入公开仓库
